.list-group
    a.list-group-item( href="javascript:void(0)" ng-class="{active: conversation._id == current}" ui-sref="conversations.view({id: conversation._id})"
        ng-repeat="conversation in conversations track by $index" ng-click="load(conversation._id)" )
        i.fa.fa-users.pull-right(ng-show="conversation.totalUsers > 2")
        i.fa.fa-lock.pull-right(ng-show="conversation.isPrivate")
        h5.list-group-item-heading
            span( ng-repeat="member in conversation.members track by $index")
                |   {{member.profile.firstName}}
                |   {{member.profile.lastName}};
        p.list-group-item-text(style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap;")
            small
                b
                    |   {{conversation.members[ conversation.lastMessage._user ].profile.firstName}}:
                |   {{conversation.lastMessage.content | limitTo : 40}}