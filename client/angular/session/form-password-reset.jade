.row(render-video-bg )
.container
    .row.login
        .col-md-6.col-md-offset-3.col-sm-8.col-sm-offset-2.col-xs-10.col-xs-offset-1.snapScreen
            .panel.panel-default
                .panel-body( ng-show="!isValidToken && ready" )
                    .alert.alert-danger.text-center(role="alert" id="valid")
                        | Your token is invalid or has expired. Please reset your password again.
                .panel-body( ng-show="isValidToken && ready" )
                    p.text-center
                        | Enter your new password
                    form(novalidate ng-submit="passwordReset()")
                        form-input-tag(
                            type="password"
                            model="user.newPassword"
                            label="{{locale['form'].label['shared'].newPassword}}"
                            required="true")

                        .form-group.ng-hide(ng-show="error")
                            .red.bold.margin {{error}}

                        button.btn.btn-danger.btn-block.btn-lg( type="submit" )
                            | {{locale['form'].button['reset']}}

                        br
                        p.text-center
                            a.btn.btn-default( href="#/login")
                                | {{locale['form'].button['cancel']}}
